app:
  applicationId: jenkins-pipeline
  env: production
  host: http://67.205.172.27:5000
hawk:
  spider:
    maxDuration: 5
  scanner:
    maxScanDuration: 30
  reports:
    - type: html
      destination: ./stackhawk-report.html
