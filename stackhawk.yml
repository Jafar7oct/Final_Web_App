app:
  applicationId: jenkins-pipeline  # Confirm this in StackHawk dashboard
  env: production
  host: http://67.205.172.27:5000  # Target server
hawk:
  spider:
    maxDuration: 5  # Limit crawl time to 5 minutes
  scanner:
    maxScanDuration: 30  # Limit scan time to 30 minutes
  reports:
    - type: html
      destination: /hawk/stackhawk-report.html  # Explicitly use /hawk/ path
